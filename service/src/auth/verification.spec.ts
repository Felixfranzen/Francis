describe('Verification', () => {
  it('generates and assigns a token to a user')
  it('rejects verification if the token is not assigned to the requesting user', async () => {})
  it('can verify a user when verification token is less than a day old', async () => {})
  it('rejects verification when token is more than a day old', async () => {})
})